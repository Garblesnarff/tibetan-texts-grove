/**
 * EmptyState Component
 * Displays a message when no translations are found
 */
export const EmptyState = () => {
  return (
    <p className="col-span-full text-center">
      No translations found. Use the upload button to add some!
    </p>
  );
};