/**
 * LoadingState Component
 * Displays a loading indicator while translations are being fetched
 */
export const LoadingState = () => {
  return (
    <div className="flex justify-center items-center h-[200px]">
      <p>Loading translations...</p>
    </div>
  );
};